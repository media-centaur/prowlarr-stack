services:
  gluetun:
    image: qmcgaw/gluetun:v3.41.3
    container_name: gluetun
    profiles: ["vpn"]
    cap_add: [NET_ADMIN]
    devices:
      - /dev/net/tun:/dev/net/tun
    environment:
      - VPN_SERVICE_PROVIDER=${VPN_SERVICE_PROVIDER}
      - VPN_TYPE=wireguard
      - WIREGUARD_PRIVATE_KEY=${WIREGUARD_PRIVATE_KEY}
      - WIREGUARD_ADDRESSES=${WIREGUARD_ADDRESSES:-}
      - WIREGUARD_PRESHARED_KEY=${WIREGUARD_PRESHARED_KEY:-}
      - SERVER_COUNTRIES=${SERVER_COUNTRIES}
      - FIREWALL_OUTBOUND_SUBNETS=${LAN_SUBNET}
      - TZ=Europe/Amsterdam
    ports:
      - "8191:8191"   # byparr solver (netns-shared)
    healthcheck:
      test: ["CMD", "wget", "-qO-", "--timeout=10", "https://api.ipify.org"]
      interval: 30s
      timeout: 15s
      retries: 3
      start_period: 30s
    restart: "no"

  prowlarr:
    image: linuxserver/prowlarr:2.5.2.5491-ls158
    container_name: prowlarr
    ports:
      - "9696:9696"
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Europe/Amsterdam
    volumes:
      - ./config/prowlarr:/config
    restart: "no"

  byparr:
    # byparr is a maintained, FlareSolverr-API-compatible Cloudflare solver
    # (same /v1 endpoint on port 8191). Prowlarr still uses its "FlareSolverr"
    # indexer-proxy type pointed at http://localhost:8191/ — no Prowlarr change.
    # It drives a real browser (Playwright-managed Firefox as of byparr 3.x,
    # Camoufox before that), so it needs more shared memory + RAM than
    # FlareSolverr did. Note: a commercial-VPN/datacenter exit IP (via gluetun)
    # is what Cloudflare weights most, so hard sites may still fail.
    image: ghcr.io/thephaseless/byparr:3.0.4
    container_name: byparr
    profiles: ["vpn"]
    network_mode: "service:gluetun"
    shm_size: "512mb"
    depends_on:
      gluetun:
        condition: service_healthy
    environment:
      - TZ=Europe/Amsterdam
    restart: "no"

  qbittorrent:
    image: linuxserver/qbittorrent:5.2.3_v2.0.14-ls475
    container_name: qbittorrent
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Europe/Amsterdam
      - WEBUI_PORT=8080
    ports:
      - "8080:8080"
      - "6881:6881"
      - "6881:6881/udp"
    volumes:
      - ./config/qbittorrent:/config
      - ${DOWNLOADS_DIR:-/mnt/videos/downloads}:/downloads
      - ${COMPLETED_DIR:-/mnt/videos/completed}:/downloads/completed
    restart: "no"

  sabnzbd:
    # Usenet download client. Runs direct (not through gluetun): usenet has no
    # peers/swarm — only the paid news-server sees your IP over TLS — so a VPN
    # would add latency for no privacy gain. WebUI on 8085 (qBittorrent owns
    # 8080). Shares qBittorrent's bind mounts so the *arr importers see one
    # completed/ tree. Credentials + API key are injected by ./setup.
    image: linuxserver/sabnzbd:5.1.2-ls271
    container_name: sabnzbd
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Europe/Amsterdam
    ports:
      - "8085:8080"
    volumes:
      - ./config/sabnzbd:/config
      - ${DOWNLOADS_DIR:-/mnt/videos/downloads}:/downloads
      - ${COMPLETED_DIR:-/mnt/videos/completed}:/downloads/completed
    restart: "no"
